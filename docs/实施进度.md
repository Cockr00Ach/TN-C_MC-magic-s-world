# 实施进度（第七系骨架铺开之后）

> 对照文档：`docs/法术总表_按设计文档.md`（设计总纲的整理版）· 详细机制见 `PROJECT-STATE.md`
> 最后更新：第七系骨架 + 93 个法术 JSON + 全量审计之后

## 一、已进代码的（可编译、已提交）

### 法术 JSON 总表（93 个，全部通过结构审计）

| 学派 | 数量 | 链 | 状态 |
|---|---|---|---|
| `LIGHTNING` 雷 | 15 | 主链 / 雷球 / 雷速 | ✅ 3/4 |
| `FIRE` 火 | 15 | 火射线 / 火球 / 燃烧 | ✅ 3/4 |
| `AIR` 风 | 15 | 风速 / 风球 / 范围风 | ✅ 3/4 |
| `WATER` 水 | 15 | 水球 / 水纹 / 水缚 | 🟡 3/4（差雨滴）|
| `EARTH` 土 | 15 | 土弹 / 土泥 / 土环 | 🟡 3/4（差土动）|
| `SOUL` 暗 | 15 | 黑夜之手 / 以伤换伤 / 黑雾 | 🟡 3/4（差召唤）|
| `ARCANE` | 3 | — | ⚠️ 旧的 `blink_1/2/3` 死文件（09-15 原型，无入口，建议删）|

**审计结果**（逐文件解析检查）：解析失败 0 · 学派异常 0 · 等级越界 0 · 缺目标 0 · 缺 impact 0 · **引用未知效果 0**

### Java 侧

| 模块 | 内容 |
|---|---|
| `TNEffects` | 雷系 4 个效果（速度 +25%/+50%、环绕标记、登神增伤）|
| `TNFireMechanics` | 火系 5 个效果（+10/25/75/150/200% 火伤）+ 燃血 / 原地复活 / 完全燃烧 / 自爆扣血 |
| `TNWindMechanics` | 风系 11 个效果（飞行标记、三档速度、两档风伤、范围风减速/加速、绕身球三档）+ 飞行机制 + 绕身球机制 |
| `SpellEngineBridge` | 法杖写入口 + **防呆闸门**（引擎不认识的法术不进法杖）|
| `StatusBarBarsMixin` | 血条/饱食度交回原版、护甲条让给我们自绘 |
| `MagicStoneHud` | 魔力条、磨砺条（护甲条，用户命名）、armor/mana 的贴图绘制 |

## 二、没做的（按原因分类）

### A. 需要研究引擎能力（下一步，风险低）

| 项 | 卡在哪 |
|---|---|
| 水系·雨滴线 5 个 | 引擎的 `Cloud` 目标字段没查（要像 BEAM/AREA 那样先看真实样例）|
| 土系·土动线 5 个 | "把敌人顶飞"的垂直位移没找到字段（`knockback` 只有水平）|
| 暗系·召唤线 5 个 | 绕身球机制的粒子写死了风的 CLOUD；要抽成参数 + 加暗系标记效果 |

### B. 需要写 Java（中等）

| 项 | 说明 |
|---|---|
| **防呆闸门第二半** | 目录里有、引擎里没有的法术：学会时就拒绝 + 界面提示"尚未实装"。要加 `Result.NOT_IMPLEMENTED`，**`MagicStoneScreen` 的两处 switch 必须同步**否则编译不过 |
| 暗系专属增伤效果 | 现在"以伤换伤"线借用火系效果（加的是 `spell_power:fire`），需要一个 `soul` 版 |
| 风神降临的尾巴 | 无冷却 + 蓝耗减半（雷系已有清冷却机制可复用）|
| 特殊魔法（文档 §12.F）| 规则魔法、无法升级、**只与亲和力有关** —— 独立机制 |

### C. 需要美术（我做得不好，建议你来做）

| 项 | 现状 |
|---|---|
| 每元素配色的血量心 | 技术方案已定：混入关掉原版心 + 我们按元素自绘（圈位由代码算）。**美术等你** |
| 魔法石卷轴界面 | 参考线底板已给：`D:\查看\原版贴图\魔法石底板_带参考线_2倍.png`（7 枝 × 6 圈，位置固定）|
| 法术图标 | 现有 45+ 张是程序生成的占位版（射线那排我自己都觉得像条纹 ✗）|

### D. 等你给规格 / 后期内容

- **雷/火/风各缺的第 4 条链** ✗（你自己编会白做，等你给）
- 风系链 2 第 5 级（`wind_spirit_lord` 是占位名）
- 文档里的后期内容：音乐、食物栏目、死亡不清饱食度、妻子/孩子好感、委托系统、Boss 取出
- **剑士/剑招**（你明确不做）

## 三、血泪经验（都踩过，别再犯）

1. **`tools/*.ps1` 必须纯 ASCII** —— 中文注释被 Windows PowerShell 按 GBK 读，会吞掉换行导致整段脚本解析失败（已犯 4 次）✗
2. **先写引擎 JSON、再进目录** —— 反过来会"学得到但放不出"，法杖多一个空槽（用户实测到过）✗
3. **学派名要查**：风是 `AIR` 不是 `WIND`；暗是 `SOUL` 不是 `DARK`；光对应 `HEALING` ✗
4. **音效 id 要查**：只有 `spell_engine:generic_<school>_casting/release`，而**风系只有 `generic_wind_charging`** ✗
5. **原版效果优先**：`absorption/resistance/regeneration/blindness/speed/strength/wither` 能覆盖大部分辅助减益；只有"精确数值的自定义增益"（风 +300%、火 +200%）才必须写效果类 ✓
6. **结构化文件不要用正则大段改写**（把 verify 脚本改坏过一次）✓ 用 edit 工具逐处改
7. **`git add -A` 会吞大文件**：仓库守卫会拦（单文件 >2 MB），世界存档/整合包一律不进 git ✓
8. **`WinRAR.exe` 是图形版**，非交互会挂死；用 `UnRAR.exe` ✓
9. **别用文件名正则猜内容**（我因此把"水 15 个"错报成 14 个 ✗）—— 要解析 JSON ✓
10. **PowerShell 两个坑**：函数别叫 `R`/`T`（撞内置别名 `r`/`t`）；`for ($x in ...)` 不合法（要 `foreach`）✓

## 四、装机状态

提交累计到 `06fc275` 为止，**最后一次成功装机是 `70c97e0` 那一批**（二级改名 + 风系）。
之后的 8 个提交（防呆闸门 + 60 条骨架 + 48 个法术 JSON）**还没进整合包** ✗ ——
装机脚本检测到 java 进程会拒绝覆盖 jar（保护机制 ✓），需要关掉游戏再装。

---

## 五、本轮更新（六系铺开之后 · 已装机可验收）

**装机状态**：整合包 jar 与编译产物**哈希一致** ✓，verify 通过 ✓，工作区干净 ✓。

### 已实装并可玩

| 内容 | 说明 |
|---|---|
| **108 个法术 JSON** | 雷/火/风各 15（3 条链）、水/土/暗各 20（4 条链）|
| **6 元素切换** | 界面底部 [雷][火][风][水][土][暗] |
| **防呆闸门** | 目录里有、引擎里没写 JSON 的法术：**放进法杖会被过滤** ✓、**学的时候被拒并提示"尚未实装"** ✓ |
| **风神降临**（风系 5 级）| 无冷却（复用雷系清冷却）+ 专属标记 `wind_god` + 蓝耗减半 |
| **暗系召唤** | 绕身球机制 + `dark_orb` 标记 → 暗影色粒子（不是白雾）；打人 + 牵引 |
| **暗系以伤换伤** | 借火系"永不致死"的燃血（代价）+ `dark_power` 真加 `spell_power:soul`（收益）|
| **射线/风刃** | 投射物模型加 `orientation=TOWARDS_MOTION` → 长条顺着弹道飞（不再"像火球"）|
| **持续场** | 土泥 / 黑雾 / 雨滴 用 `CLOUD`（每 N tick 反复施加），不是"打一发" |
| **主城** | Medieval Town 已进整合包 saves/（出生点 0/64/0，生存）|

### 还没做

| 项 | 卡在哪 |
|---|---|
| 特殊魔法（领域魔法）**第 2~5 步** | 目录层已建 ✅；数据层/命令/亲和力加成/界面待做。**需要你定**：每点亲和力给多少加成（建议 +5%/点）、七个元素各自的领域名与效果 |
| **雷/火/风各缺的第 4 条链** | **需要你给规格**（文档里只有水/土/暗是 4 条）|
| **风系链 2 第 5 级** | 现占位名「风灵之主」，**需要你定内容** |
| 暗系召唤物做成**独立实体** | 需要自定义实体 + AI（现在是绕身球过渡版）|
| 委托系统 / 音乐 / 食物 / 妻子好感 / Boss 取出 | 文档里的后期内容，明确不在本轮范围 |
| 剑士与剑招 | 你明确说先不做 |

### 交付文档索引

- `docs/法术总表_按设计文档.md` —— 全部元素的链与法术名（对照设计总纲）
- `docs/实施进度.md` —— 本文（做到哪、卡在哪、为什么）
- `docs/特殊魔法_设计.md` —— 领域魔法方案（三条硬规则 + 三种加成方案 + 五步）
- `PROJECT-STATE.md` §6 引擎能力补充（三种模板 / CLOUD / AREA / BEAM / orientation / 原版效果 / 学派名 / 音效）
- `PROJECT-STATE.md` §7 方法论六条（借效果 / 枚举 switch / 生成器 / 编译守卫 / 先查字段 / 别凭印象数）

---

## 六、特殊魔法（领域魔法）实现进度 · §12.F

**文档要求**：无法升级、获得路线即获得、**强度只与亲和力有关**。

| 步骤 | 状态 |
|---|---|
| 1. 目录层（`SpellCatalog.Special` + 七元素条目） | ✅ **不进 Chain**（领域魔法没有 1~5 级，塞进去会破坏"每条链 5 级"的自检） |
| 2. 数据层（`MagicStoneData.specials` + NBT + 4 个方法） | ✅ 存元素 id（改枚举顺序也不串位）；老存档无该键 → 空集，向后兼容 |
| 3. 命令层（`/tnc special grant|revoke|list`） | ✅ 元素参数认 id / 枚举名 / 中文名 |
| 4. **亲和力 → 强度**（`TNSpecialMagic`） | ✅ 已获得时给 `spell_power:<学派>` 挂 = 亲和力 × 5%（可调常量） |
| 5. 界面展示 + 七元素法术本体 | 🟡 `/tnc show` 已有"领域魔法"一行 ✅；**每元素一个法术 JSON 待用户定效果** ✗ |

### 第 4 步的两个坑（都处理了）

1. **亲和力变了要重算** —— 只算一次的话，`/tnc affinity set` 之后加成不会变 ✗。
   现在每 5 秒重算（开销可忽略），并且**数值没变就不动属性** ✓。
2. **必须用 transient 修饰符** —— 永久修饰符会写进玩家 NBT ✗，改亲和力时清不干净 ✗，会越叠越多 ✗。
   transient 只活在内存、登录后重算 ✓。用**固定 UUID**，重复施加是替换不是叠加 ✓。

### 学派名映射（这里差点出错）

`Element.id()` **不能**直接当学派名：光 = `light` ✗ 引擎是 **`healing`** ✓；暗 = `dark` ✗ 引擎是 **`soul`** ✓。
写错的表现是"属性取不到 → 加成静默失效" ✗ —— 已在 `TNSpecialMagic.schoolOf()` 里硬编码正确映射（依据 §6.7）。

### 这一小时新增的工具与守卫

- `verify_mod_jar.ps1`：法术 id 清单扩到 35 条、效果白名单扩到 20 个（新效果要及时补，否则误报 ✗）
- **编译守卫**：所有构建命令都带 `if (BUILD FAILED) { 回滚; exit }` —— 累计拦下 **6 次**编译失败 ✓，
  其中一次是"锚点凭记忆写错"✗、一次是"抓第一个 `return checks` 抓错位置"✗
