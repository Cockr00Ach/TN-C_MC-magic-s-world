> **维护者**：**法术系统YYY** · **最后更新** 2026-09-20
> **归属**：本文件由 **法术系统YYY** 维护 ✓；其他专题请勿在此改状态 ✗ —— 全局状态统一在 `当前状态.md`（法术系统YYY 维护）。
> **跨专题约定**：地图/结构类文档由地图专题维护（如 `地图接入进度.md`）；如需联动请在交接里写明 ✓。

> **跨专题约定（编剧系统 AAA 加笔 2026-09-20）**：**剧情/任务/文案**由 **编剧系统 AAA** 维护 ✓ —— 唯一事实来源是工作区根目录 `剧情总纲.md`；
> 其他专题写任务文案前请先读它，术语与口吻以它为准 ✓。
# 当前状态（唯一事实来源）

> **本文是"现在到底做到哪了"的唯一权威** ✓ —— 其他文档里的"现状/进度"段落都是**写作时点的快照** ✗，
> 如果与本文冲突，**以本文为准** ✓。
> 最后更新：**2026-09-20** · 维护规则：**每次收尾/交接前更新这一页** ✓

---

## 一、模组本体（TN-C）

| 项目 | 状态 | 备注 |
|---|---|---|
| 编译 | ✅ 通过 | `$env:JAVA_HOME='D:\ModTest\.jdk17'` → `.\gradlew.bat build` |
| 装机 | ✅ 已装最新 | `<整合包>\mods\tnc-1.0.0.jar`，**逐项核对过 jar 内容** ✓ |
| verify | ✅ 通过 | `tools\verify_mod_jar.ps1` |
| 仓库 | 🟡 工作区干净，**领先 5 个提交未推送** | `## main...origin/main [ahead 5]`（含剧情道具的 3 个提交＋其他专题的 2 个）；用户要求暂不推送 |

## 二、法术体系（详见 `法术专题_交接.md`）

| 项目 | 状态 |
|---|---|
| 六系法术 | ✅ **108 个**：雷/火/风各 15（3 链）、水/土/暗各 20（4 链）|
| 魔法石界面 | ✅ 改版完成（深棕+金框 / 用户水晶 / 法术图标 / 未学乱码 / 悬停六行提示）|
| 领域魔法（§12.F）| 🟡 **4.5/5**（目录/数据/命令/亲和力加成/`show` 显示 ✓；缺"每元素一个领域法术本体"，等用户定名字与效果）|
| 防呆闸门 | ✅ 学不到未实装法术 + 不进法杖空槽 |
| 风神降临 | ✅ 无冷却 + 专属标记 + 蓝耗减半 |
| 暗系增伤 | ✅ `dark_power`（真加 `spell_power:soul`）|
| 投射物模型 | ✅ **流程打通**（`docs\投射物模型_配方.md`）；现有用户自制模型：`fireball` / `waterball` / `lightingball` / `thunder_ball`；**名单只有一份** ✓ `TNProjectileModels.PROJECTILE_MODELS`（`TNModelBaking` 直接读它，2026-09-21 清掉 7 个没文件的空条目 ✗）|

## 三、剧情道具（两类可阅读纸：宝箱传说残卷 + 一周目正史记录纸）

### 3.1 宝箱传说残卷（六卷）

内容出处：`剧情\宝箱传说残卷.md`（**文书专题**维护，别改）。**道具本体由法术系统YYY 实装** ✓。

| 道具 id | 名字 | 稀有度 |
|---|---|---|
| `tnc:canjuan_1a` | 宝箱传说残卷 · 卷一（上）挥戈 | 青 UNCOMMON |
| `tnc:canjuan_1b` | 宝箱传说残卷 · 卷一（下）归 | 青 UNCOMMON |
| `tnc:canjuan_3` | 宝箱传说残卷 · 卷三 衍 | 蓝 RARE |
| `tnc:canjuan_4` | 宝箱传说残卷 · 卷四 垣 | 蓝 RARE |
| `tnc:canjuan_5` | 宝箱传说残卷 · 卷五 代 | 蓝 RARE |
| `tnc:canjuan_6` | 宝箱传说残卷 · 卷六 坐望 | 紫 EPIC |

- 实装：`src\main\java\com\tnc\tnc\magic\TNScrolls.java`（六卷 `stacksTo(1)`）＋ `TNScrollItem.java`（**右键阅读** ✓）＋ `client\TNScrollScreen.java`（阅读界面）＋ `assets/tnc/models/item/canjuan_*.json`（原版 `item/generated` 平面模型）＋ `assets/tnc/textures/item/canjuan_*.png`（**统一一张"残页"**，方案 A：一部残卷散成碎片的观感）＋ `lang` 中英文名。
- **右键阅读** ✅ **已实装**：复用**原版书本界面**（`BookViewScreen`）——不画新 UI、不加贴图，翻页/排版/翻页音效都是原版行为 ✓；正文按 114px 折行后**每页 13 行**（原版一页最多 14 行，超出会**静默裁掉** ✗）；正文里的 **「」** 按编剧要求上**金色** ✓；物品提示行"右键阅读" ✓。
- **正文的唯一来源**：`剧情\宝箱传说残卷.md` 的 **【卷文】段**（**文书专题**维护）→ 由 `tools\gen_scroll_lang.ps1` 生成 lang 键 `scroll.tnc.<id>`。**改文案不用碰 Java** ✓：改 md → 跑脚本 → 重新编译装机。
- ⚠️ **【暗扣】段（对应角色 / 埋的钩子 / 真名）绝不进游戏** ✗：脚本只取每个卷的**第一段**；装机后核对过 `暗扣 / 吴归衡 / 公孙衍 / 熙永槐 / 庄鹊让 / 周坐望` 在 jar 内出现 **0 次** ✓（`鲁阳公` 是正文里作者故意留的唯一真名 ✓）。
- **可直接验收**：已在 TN-C 创造页里（`itemGroup.tnc.main`），不用 `/give` ✓；**拿在手上右键就能读** ✓；外观预览图 `docs\宝箱传说残卷_物品预览.png`。
- ⚠️ **注册时机的坑**：`DeferredRegister` 由 `TNMod` 构造函数显式 `TNScrolls.register(modEventBus)` 挂总线，**不能**靠静态块 ✗（Java 类懒加载 → 静态块不跑 → 物品静默消失，jar 里有模型有贴图但游戏里没有）。
- 🟡 **仍缺"箱子掉落"**：道具本体 + 阅读已完成 ✓，**掉落配置属于任务系统 / 文书专题** ✗，那边接。道具 id 已固定，别改名 ✗。

### 3.2 一周目正史记录纸（五张：前情 ＋ 主线四段）

作者 2026-09-22 要求：**"类似残卷那样，记录主线四场的主要剧情和前情"** ✓ 已实装。

| 道具 id | 名字 | 稀有度 | 正文字数 |
|---|---|---|---|
| `tnc:zhengshi_qianqing` | 一周目正史 · 前情 | 青 UNCOMMON | 112 |
| `tnc:zhengshi_1` | 一周目正史 · 一 · 分离之后 | 蓝 RARE | 68 |
| `tnc:zhengshi_2` | 一周目正史 · 二 · 黑暗潮 | 蓝 RARE | 60 |
| `tnc:zhengshi_3` | 一周目正史 · 三 · 黑暗深处 | 紫 EPIC | 80 |
| `tnc:zhengshi_4` | 一周目正史 · 四 · 挥戈 | 紫 EPIC | 122 |

- **机制完全复用残卷那套** ✓：`TNRecords` 里 5 个 `TNScrollItem`（**右键读** + 原版书本界面 + 「」金色），代码不再重复 ✓。
- **正文唯一来源**：`剧情\主线概要.md`（**编剧系统 AAA** 维护）→ `tools\gen_records_lang.ps1` 生成
  `scroll.tnc.zhengshi_*`（正文）与 `item.tnc.zhengshi_*`（名字）✓。**改文案不用碰 Java** ✓。
  文档路径写在 `tools\records_lang_extra.json` 的 `doc` 里（脚本保持纯 ASCII ✓）。
- **外观**：五张共用 `assets/tnc/textures/item/zhengshi.png`（取自 MCA 的"文书"＝两张纸＋火漆印）——
  **故意和残卷的单张撕页区分开** ✓，一眼能分辨两个道具集。预览图 `docs\一周目正史_物品预览.png` ✓。
- ⚠️ **元注释会被剥掉** ✗：概要是**交接文档**，里面有几处是写给制作组看的；脚本按
  `records_lang_extra.json` 的 `strip` 名单删除并**在输出里列出来**（当前 2 处：
  `（★ 真相全在这里，见正文）`、`（最终 boss，她只是等在那儿）`）✓ 不是静默修改。
- ⚠️ **剧透/发放时机要定** ✗：段三、段四两张写的就是结局（**归杀衍 / 归消失**）。若当作前期掉落物会直接剧透 ✗ ——
  建议**通完某段才给那张**（发放归任务系统），或者你要我改成"只写到当前进度"的版本 ✓ **等你定**。
- ⚠️ **验证脚本的守卫要分清对象**：残卷的"**正文绝不点名**"守卫现在**只扫 `canjuan_*`** ✓ ——
  正史纸按编剧设计就是全名叙述（`吴归衡/公孙衍/…`），不能被那条守卫误伤 ✗（已做反证测试 ✓）。

## 四、庄鹊让（卷五《代》角色）· 剧情 NPC ＋ 二周目可玩角色（作者 2026-09-22 指定）

作者原话：**"一周目是站着对话的剧情 NPC；二周目主角消亡之后，她变成可供玩家游玩的角色 ——
玩家可以选择成为她，如果没选择她，那她还是 NPC。"**

### 4.1 ✅ 一周目：剧情 NPC（**已实装**，2026-09-22）

| 项 | 内容 |
|---|---|
| 实体 | `tnc:zhuangquerang`（`ZhuangquerangNpcEntity extends TnDialogueNpc`）—— 站着不动、不会消失、打不死、**空手右键对话** ✓ |
| **模型** ★ | **保持女仆模型** ✓（作者 2026-09-22 要求）：装了 GeckoLib 时实体是子类 `ZhuangquerangMaidNpcEntity`，客户端用 **GeckoLib 直接画 TLM 的女仆模型** ✓ —— 与"Change Skin"里看到的是**同一个模型文件**（十六夜咲夜 bedrock 1.12.0）|
| 三个资源 | `assets/tnc/geo/entity/zhuangquerang.geo.json`（16881 B，与 TLM 模型**逐字节一致** ✓）＋ `textures/entity/zhuangquerang.png`（128×128 女仆图集 ✓）＋ `animations/entity/zhuangquerang.animation.json`（**我们自己写的**轻微 idle：只动 `head` 左右各 3°）✗ |
| ⚠️ 动画的坑 | TLM 的 `maid.animation.json` **用不了** ✗ —— 它是 GeckoLib 骨架（`Root/UpBody/LeftArm…`），而咲夜是旧格式骨架（`head/body/armRight…`），**骨骼名对不上**（实测确认）；TLM 自己是靠内置 **JS 动画引擎**（`javax.script`）且只对 `IMaid` 实体生效 ✗。所以姿态是"模型自带站立姿态 + 我们的小 idle" |
| 降级路径 | 没装 GeckoLib → 注册父类，走 NPC 专题的原版人形模型 ＋ `textures/entity/zhuangquerang_humanoid.png`（64×64，由 `tools\gen_zhuangquerang_skin.ps1` 生成，蓝白女仆配色）✓ —— **分流点** `npc\compat\MaidNpcSupport`（整个 mod 里唯一提到 GeckoLib 的地方，避免"没装就整个 mod 加载不了" ✗）|
| 尺寸旋钮 | 游戏里觉得她偏大/偏小/悬空 → 只改 `ZhuangquerangMaidRenderer.MODEL_SCALE` / `MODEL_Y_OFFSET` 两个常量 ✓（默认 1.0 / 0，模型高约 1.6 格）|
| 构建依赖 | `libs\touhoulittlemaid-1.5.2.jar`、`libs\geckolib-4.8.4.jar` ＋ `build.gradle` 里 `compileOnly fg.deobf(...)` —— ⚠️ **`fg.deobf` 不认 `files(...)`** ✗，必须先加 `flatDir { dir 'libs' }` 仓库（否则只是警告 + 用未重映射的 jar）；两个 jar **只用于编译**，不会打进我们的 jar ✓（`compileOnly`）|
| 剧本 | `data/tnc/dialogues/zhuangquerang_first.txt` → `@next` 接力 `zhuangquerang_second.txt` ✓ —— **任务系统 BBB 已代拟第二段并把主题色设成蓝调**（`DialogueTheme` → blue）✓，台词归编剧系统 AAA ✗ |
| 位置 | `NpcPlacementDefaults`：`CENTER (+15, 0, -13)` —— **暂定** ✗（贴着用户实测过的 `huai` 放，免得生成进墙里）；定位置用 `/tnc npc here zhuangquerang` 再把 `list` 那行抄回去 ✓ |
| 默认表版本 | `NpcPlacementDefaults.VERSION` **2 → 3** ✓（不改这个，老存档不会自动补位 ✗）|
| 加一个 NPC 要碰的 **6 处** | ① `TNNpcs` 注册实体 ② `TNNpcAttributes` 刷怪蛋 ③ 同文件 `onEntityAttributeCreation`（漏了 = 一生成就抛"has no attributes" ✗）④ `TNNpcClientEvents` 渲染器（漏了 = **隐形** ✗）⑤ `TNMod` 创造页 ⑥ `NpcCommand` 的 **`purge all` 硬编码名单** ✗（漏了 = purge 漏掉她）＋ ⑦ 默认位置表 ＋ ⑧ lang 名字 |
| 那个"女仆同伴" | `tnc_pet:zhuangquerang`（TLM 模型包里的条目）**保留** ✓ —— 现在她的一周目 NPC 直接用同一个模型文件，那个包条目留着给"将来当跟队同伴"用 ✓ 见 4.3 |

### 4.2 🟡 二周目：可选可玩角色（**未实装** ✗，等作者拍板）

**约束（查过，不是猜的）**：
- 包里**没有**换肤/换模型 mod ✗（只有 `skinlayers3d` = 3D 皮肤层，改不了皮肤内容）；
  **玩家外观无法直接变成她** —— TLM 的 128×128 女仆图集也**不能**当玩家皮肤用（实测：既不是原版布局也不是 2× 缩放 ✗）。
- 二周目"选角色"这套**代码一行都没有** ✗；`剧情/Mc - Idea_TN-C剧情.md` §二周目剧情设计 里作者已写过
  "**玩家可以选择不同剧情人物**"、玩法细节 2"**开局可选不同的角色人物**（后续通过委托解锁其他角色才拥有的能力）" ✓。

**要作者定的 4 件事**：
1. **"成为她"到哪一步**：只换身份/亲和力/专属法术（不动外观，零新 mod）／还是要外观也变（得加皮肤 mod 或 Yes Steve Model，我可以把 TLM 模型转过去）？
2. **选择入口**：`/tnc character` 命令 + 我写一个选择界面／由 NPC 对话触发／做成 FTB 任务？
3. **周目怎么切**：先做 `/tnc playthrough 2` 手动切／主线任务完成时自动切？
4. **她的数值**：亲和力七系与初始点数（我按设计文档的"战士全 1 / 法师 16 点 / 主角七系全 3=21 点"规则给建议值）。

**已想好的落地骨架**（等上面拍板就能开工）：周目状态存**世界级** `SavedData`（照 `NpcPlacementSavedData` 的写法）→
`选择 = 应用她的亲和力/点数/专属法术预设`（`MagicStoneData.setAffinity` / 加点 API 都有 ✓）→
**没选她 ⇒ 默认 NPC 照常生成；选了她 ⇒ 她的 NPC 不再生成**（`NpcPlacementDefaults` 里按"是否被选"过滤一行即可）✓。

### 4.3 两条形态的关系（别做重）

| 形态 | 是什么 | 现状 |
|---|---|---|
| **剧情 NPC**（`tnc:zhuangquerang`）| 站在天空岛上、右键对话 | ✅ **已实装**（4.1）＝作者要的"一周目形态" |
| **TLM 女仆**（`tnc_pet:zhuangquerang`）| 会跟随的同伴实体，借十六夜咲夜外形 | ✅ 已装好，**留着备用**（模型好看、现成的，将来她要是当"跟队同伴"就能用）|

**蓝白女仆配色的来源**：作者要求"蓝色调、好看"，我从 189 个 TLM 模型里按蓝色像素占比全扫了一遍
（天子 64% / 琪露诺变体 62% / 紫苑 56% / 咲夜 12%），选**十六夜咲夜**是因为她"**忠心女仆**"的形象与
庄鹊让「**门客 / 代主受难**」的定位同构 ✓；候选对比图 `docs\庄鹊让_候选模型对比.png` ✓。
**TLM 模型授权**：CC BY-NC-SA 4.0（非商用、需署名）→ 复制品同性质 ✓ 只能用于免费整合包 ✗。

## 五、其他专题

| 专题 | 状态 | 文档 |
|---|---|---|
| 主城 / 地图 | ✅ 已合并队友的**天空岛新手村**（结构生成 + 双向传送阵 + 出生点，14 个提交）| `地图接入进度.md` |
| 全部命令 | `/tnc show / mana / affinity / points / progress / learn / forget / forgetall / spells / special / selftest` | `项目交接.md` |
| 女仆模型包 | ✅ `tnc_pet`（`songzhikun` ×2 ＋ **`zhuangquerang` 庄鹊让**）；**不在 git 里** ✗，靠 `tools\maid_models.json` + `tools\sync-tlm-models.ps1` 重建 | 本文第四节 |

## 六、待办（按优先级）

1. **投射物模型补全**：射线模型（用户导出 `fireray`）→ 土/暗/风各一个（`earthball`/`darkball`/`windblade`）；`waterball` 换用户自己的形状
2. **法术图标**：只做了 **35/105** ✗，缺的显示品红方块 → `美术资产清单.md` 列了缺哪些
3. **特殊魔法收尾**：每元素一个领域法术本体（**等用户定名字与效果** ✗）
4. **雷/火/风各缺第 4 条链**（**等用户给规格** ✗，别自己编）
5. 风系链 2 第 5 级内容（占位"风灵之主"）
6. 暗系召唤线做成真召唤实体（现在是绕身球过渡版 ✗）
7. 收尾清理：删 `.bak`（berserker jar 备份）、空壳资源包 `TN-C-Models.zip`
8. 残卷**箱子掉落**（道具本体 + 右键阅读已实装 ✓，id 见第三节）→ **任务系统 / 文书专题**的活，法术侧不再动
9. 庄鹊让**还是借用十六夜咲夜的模型+贴图**（见第四节）；两种收尾法：① 照 `songzhikun` 的路子画自己的贴图 ② 换一个更贴的蓝调模型（改一行 + 跑脚本 ✓）—— **等用户拍板** ✗
10. songzhikun 贴图未画（一直用 UV 验证图，见 `PROJECT-STATE.md` §4.3）

## 七、铁律（每条都真踩过，详见 `法术专题_交接.md` 第六节）

```
① JAVA_HOME 用 D:\ModTest\.jdk17；【不要只看 identical】✓ 要核对 jar 内容
② 打包 zip 必须正斜杠条目名 ✗ 反斜杠 MC 读不了
③ 用了模型的法术必须在 mod jar 的 data/ 里 ✗ 别只放 kubejs（会被覆盖）
④ 不要改 Fabric 模组 jar ✗（Connector 重映射会崩）
⑤ sync.cmd 是双向的 ✗ 删完 kubejs 文件别再 sync
⑥ 投射物贴图必须在 textures/spell_projectile/ ✗ 不是 projectile/
⑦ tools/*.ps1 必须纯 ASCII ✗（本机 PowerShell 是 5.1，读无 BOM 的 .ps1 按 ANSI → 中文字面量必乱码 ✗；中文放 .json 数据文件，用 [IO.File]::ReadAllText + UTF8 读 ✓）
⑧ 跑 tools 里的脚本要 `-ExecutionPolicy Bypass` ✗ 否则 "running scripts is disabled"；本机 pwsh 不在 PATH，用 powershell ✓
⑨ DeferredRegister 必须由 TNMod 构造函数显式挂总线 ✗ 靠静态块会因类懒加载静默不注册
```

## 八、文档地图（谁在什么时候写的）

| 文档 | 性质 | 可信度 |
|---|---|---|
| **`当前状态.md`（本文）** | **技术**唯一事实来源 | ✅ 最新 |
| **`../剧情总纲.md`**（工作区根目录） | **剧情/任务/文案**唯一事实来源（世界观·主角·五阶·领域魔法·命名规范）| ✅ 最新 |
| `法术专题_交接.md` | 法术专题**入口**（工作流/内容/配方/坑）| ✅ 最新 |
| `投射物模型_配方.md` | 6 步配方 + 紫黑排查 | ✅ 最新 |
| `法术总表_按设计文档.md` / `美术资产清单.md` / `法术制作与测试.md` / `特殊魔法_设计.md` | 参考类（内容长期有效）| ✅ 有效 |
| `实施进度.md` / `项目交接.md` / `验收表.md` | **写作时点快照**（2026-09-19 左右）| ⚠️ 现状以本文为准 |
| `PROJECT-STATE.md` §6/§7 | 引擎能力 + 方法论（长期有效）| ✅ 有效 |
| `地图接入进度.md` | 地图专题（队友维护）| ⚠️ 由该专题维护 |
| `任务系统_交接.md` | **任务系统BBB** 维护：FTB 任务实施侧作业手册（含"config 不进 git"的坑）| ✅ 最新 |
| `../archive/author-quest-content/` | **任务系统BBB** 维护：作者旧任务章节的**删除前唯一快照** | ✅ 归档 |
| `宝箱传说残卷_物品预览.png` | 六卷残卷的**物品外观预览**（法术系统YYY 生成）| ✅ 与 jar 内贴图同源 |
