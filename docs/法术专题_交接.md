> **维护者**：**法术系统YYY** · **最后更新** 2026-09-20
> **归属**：本文件由 **法术系统YYY** 维护 ✓；其他专题请勿在此改状态 ✗ —— 全局状态统一在 `当前状态.md`（法术系统YYY 维护）。
> **跨专题约定**：地图/结构类文档由地图专题维护（如 `地图接入进度.md`）；如需联动请在交接里写明 ✓。

# 法术系统YYY · 法术专题交接文档（TN-C 魔法体系）

> **本专题范围**：TN-C 模组的魔法体系（魔法石 / 法杖 / 六系法术 / 领域魔法 / 投射物模型）。
> 下一个话题请从这份文档开始。相关文档在文末索引。

---

## 一、现状一句话

**108 个法术、6 个元素、每系 3~4 条链全部可玩** ✓；魔法石界面改版完成 ✓；领域魔法（设计文档 §12.F）完成 4.5/5 ✓；
投射物模型的制作流程**已打通并验证**（火球/水球/雷球已用用户自制模型 ✓）。装机最新 ✓、verify 通过 ✓、工作区干净 ✓。

---

## 二、日常工作流（5 条命令，照抄）

```powershell
# 0) 必须先设 JAVA_HOME（⚠️ 不要用 .minecraft\runtime 那个，PCL 会占用它导致 gradle 静默不编译 ✗）
$env:JAVA_HOME = 'D:\ModTest\.jdk17'          # 项目内固定 JDK17 副本 ✓

cd D:\ModTest

# 1) 编译（改了 Java 才需要）
.\gradlew.bat build

# 2) 装机（游戏开着会被拒 ✗ —— 这是保护机制 ✓；PCL 的 java 进程也会挡，可跳过脚本直接复制）
#    脚本:  powershell -ExecutionPolicy Bypass -File tools\install-to-pack.ps1
#    直接:  Copy-Item D:\ModTest\build\libs\tnc-1.0.0.jar <整合包>\mods\tnc-1.0.0.jar -Force

# 3) 只改法术 JSON / 模型贴图 -> 不需要装机：
#    · 法术 JSON 在 mod jar 里 -> 要重新编译+装机（见上）
#    · 资源包/贴图类 -> 同步 + 重进游戏

# 4) 同步 + 验证
cd D:\ModTest\modpack ; .\sync.cmd push        # ⚠️ 这是【双向】同步 ✗ 见第六节
cd D:\ModTest ; powershell -ExecutionPolicy Bypass -File tools\verify_mod_jar.ps1

# 5) 【只在改了剧情文案时】重新生成残卷正文 -> 再编译装机
#    正文的唯一来源是 剧情\宝箱传说残卷.md 的【卷文】段（文书专题维护）
powershell -ExecutionPolicy Bypass -File tools\gen_scroll_lang.ps1
powershell -ExecutionPolicy Bypass -File tools\gen_scroll_lang.ps1 -Check   # 只解析不写
```

**铁律**：**不要只看 `identical: True`** ✗ —— 构建失败时它只是"两个旧文件互相比" ✓。
**永远核对 jar 内容** ✓（本专题的所有"改了没反应"事故都源于此 ✗）。

整合包路径（PCL 版本隔离，游戏目录 = 版本目录）：
```
E:\download\正式版 2.12.6.1\.minecraft\versions\元素觉醒1.4.3-魔改版-20260915\
    mods\tnc-1.0.0.jar          ← 我们的模组
    kubejs\data\tnc\spells\     ← 数据包法术（⚠️ 不要与 jar 内的同名 ✗）
    config\openloader\...       ← 资源（本项目已弃用这条路径，见第六节）
```

---

## 三、内容清单（做了什么）

### 3.1 法术（108 个，全部在 kubejs 或 mod jar 的 data/tnc/spells/）

| 系（引擎 school）| 链 | 数量 | 状态 |
|---|---|---|---|
| 雷 `LIGHTNING` | 主链 / 雷球 / 雷速 | 15 | ✅ 3/4（第 4 链等用户给规格）|
| 火 `FIRE` | 火射线 / 火球 / 燃烧 | 15 | ✅ 3/4 |
| 风 `AIR` | 风速 / 风球 / 范围风 | 15 | ✅ 3/4 |
| 水 `WATER` | 水球 / 水纹 / 水缚 / 雨滴 | 20 | ✅ 4/4 |
| 土 `EARTH` | 土弹 / 土动 / 土泥 / 土环 | 20 | ✅ 4/4 |
| 暗 `SOUL` | 黑夜之手 / 以伤换伤 / 召唤 / 黑雾 | 20 | ✅ 4/4 |

详见 `docs/法术总表_按设计文档.md`（对照设计总纲的逐条清单）。

**⚠️ 重要约定**：**用了投射物模型的法术必须放在 mod jar 的 `src/main/resources/data/tnc/spells/`** ✓，
不能只放 kubejs ✗（数据包后加载 → 引擎收集模型时看不到它们 → 紫黑）。当前 jar 内有 34 个，kubejs 有 74 个。

### 3.2 机制（Java 侧）

| 类 | 负责 |
|---|---|
| `TNEffects` | 雷系效果 + `dark_power`（暗系专属增伤，加 `spell_power:soul`）|
| `TNFireMechanics` | 火系 5 档增伤 + 燃血（永不致死）/ 原地复活 / 完全燃烧 / 自爆扣血 |
| `TNWindMechanics` | 飞行（4 级起永久）/ 三档速度 / 两档风伤 / 范围风减速加速 / 绕身球 + 牵引 / `dark_orb` 暗系粒子 |
| `TnSpellMechanics` | 施法瞬间（环绕/拖尾/清冷却）—— 雷系"闪电登神"与风系"风神降临"共用无冷却 |
| `MagicStoneLearning` | 解锁规则（含防呆闸门 `NOT_IMPLEMENTED`）|
| `MagicStoneData` | 亲和力/魔力/点数/链进度/已学/领域魔法（全部 NBT 持久化）|
| `TNSpecialMagic` | 领域魔法强度 = 亲和力 × 5%/点（挂在 `spell_power:<school>`，每 5 秒重算）|
| `MagicStoneHud` / `MagicStoneScreen` / `AffinityWidget` | 魔力条/磨砺条 / 魔法石界面（深棕+金框）/ 亲和度水晶控件（用户贴图 + 42 个点位）|
| `TNSpellClientVisuals` | **客户端**：雷速链的视野拉伸（`ComputeFovModifierEvent`，挂 FORGE 总线 + Dist.CLIENT）|
| `SpellEngineBridge` / `SpellEngineManaHook` | 与 SpellEngine 的桥接（法杖写入 + 施法扣蓝 + 防呆闸门）|

#### 3.2b 雷速链 5 档的"看得见"表现（2026-09-22，作者要求：自身增益也要有体现）

SELF 型法术在引擎里**没有任何模型/投射物** ✗，所以表现全放在 `TnSpellMechanics` 的每 tick 逻辑里（框架早就有了，只是没往里加 case）：

| 档 | 法术 | 表现 | 可调常量（都在 `TnSpellMechanics` 顶部）|
|---|---|---|---|
| t1 | `lightning_haste` | **跑动时脚底电花**（站着只偶尔闪）+ 视野 +3% | `HASTE_SPARK_INTERVAL`(4)、`HASTE_MIN_MOVE` |
| t2 | `lightning_blink` | **起点 + 落点各炸一圈电花 + 闪光（FLASH）** + 两声短促传送音 | `BLINK_BURST_COUNT`(28)、`BLINK_BURST_SPREAD`、`BLINK_MIN_DISTANCE_SQR` |
| t3 | `lightning_wind` | 冲刺**电弧拖尾 + 速度线（END_ROD）** + 撞到敌人触电 4 伤 + 视野 +8% | `WIND_TRAIL_INTERVAL`(2)、`WIND_HIT_RADIUS`、`WIND_DAMAGE`、`WIND_MIN_MOVE` |
| t4 | `lightning_recharge` | 施法**一圈电环**（14 点）+ 之后 **3 秒绕身上升电流** + 晶鸣 | `RECHARGE_FLOURISH_TICKS`(60)、`RECHARGE_RING_COUNT/RADIUS` |
| t5 | `lightning_ascension` | **全身随机电弧 + 三颗环绕电光（纯表现不伤人）+ 每走一格留一枚雷印**（存活 40 tick）+ 视野 +5% | `ASCENSION_ARC_INTERVAL/COUNT`、`ASCENSION_ORBIT_*`、`ASCENSION_MARK_LIFE/STEP/MAX` |

- **效果颜色改成雷的青色** ✓：`TNEffects.COLOR_LIGHTNING` `0x7C5CE0`(紫) → `0x9FE8FF`；`lightning_wind` → `0x7FE8FF`；登神 → `0xCFF4FF`。这个颜色同时决定 **HUD 图标底色** 和 **身上那圈效果粒子颜色** ✓。
- **粒子：用包里的"真电弧"，不是原版小亮点** ★（2026-09-22 作者反馈"怎么是星星，丑死了"后改的）：
  `arc()` 按顺序找 `spell_engine:electric_arc_b` → `electric_arc_a` → `alexscaves:tesla_bulb_lightning`
  → `aquamirae:electric` → `jerotes:chain_lightning_display` → `berserker_rpg:small_thunder`，
  取第一个能解析到的（**懒解析**：注册表构造期还没填满 ✗），一个都没有才退回 `ELECTRIC_SPARK`，
  并**在日志里写明用了哪个** ✓（`TN-C: 雷速特效使用电弧粒子 xxx`）。
  观感关键不在粒子本身，而在**画法**：`arcLine()` 沿一条抖动的线撒点（星点散着撒永远像星星 ✗），
  `arcBall()` 画球状炸开。**原版闪电实体不能用** —— `LightningBolt` 不管 `visualOnly`
  都会播 10000 音量的雷声（读源码确认 ✗）。
- 音效一律原版（`ENDERMAN_TELEPORT` / `AMETHYST_BLOCK_CHIME`）✓；只有 `FLASH` 还用原版（闪现的亮光）。
- 视野拉伸取**最高档**而不是相乘 ✗（三样叠加会晕）：`TNSpellClientVisuals` 里 `FOV_LIGHTNING_HASTE/WIND/ASCENSION = 1.03/1.08/1.05`。
- 内存里的临时状态（回蓝窗口 / 雷印 / 上一个雷印位置）都是 `ConcurrentHashMap<UUID, …>`，**不进存档** ✓；雷印到期自动清、buff 消失时清"上一个位置" ✓。

#### 3.2d 环绕雷球 = **真实体**（2026-09-22 作者："环绕雷球的技能，变为我们的实体雷球啊"）

以前"环绕"是拿 `ELECTRIC_SPARK` 粒子在玩家周围画圈 ✗（看着像一圈亮点，不是我们的球）。现在是真实体 ✓：

| 项 | 内容 |
|---|---|
| 实体 | `tnc:thunder_orb`（`magic\TNThunderOrbEntity`）—— `Entity` 子类，**无 AI / 无重力 / 不参与方块碰撞**，位置每 tick 自己算 ✓ |
| 注册 | `magic\TNOrbEntities`（**法术产物**的注册表，与剧情 NPC 的 `npc\TNNpcs` 分开 ✗）；在 `TNMod` 构造函数里 `register(modEventBus)` ✓ |
| 外观 | 与投射物**同一个模型**：`client\TNThunderOrbModel` 照着 `assets/tnc/models/projectile/lightingball.json` 的 **7 个方块**用代码搭（核心 4³ ＋ 六面各一块"尖"）＋同一张贴图 ✓；渲染器 `client\TNThunderOrbRenderer` **全亮度**画（对应投射物的 `light_emission: RADIATE`）＋自转 ✓ |
| 坐标换算 ⚠️ | 模型空间 **+Y 朝下**（与 JSON 相反 ✗）→ `modelY = -(jsonY - 10)`；球心 JSON `(8,10,9)` 平移到原点 ✓。**UV 故意不较真**：`lightingball.png` 是纯色贴图（只有 1 种颜色）✓；换带图案的贴图（如 `thunder_ball.png`）就得按 per-face uv 重算 ✗（那时建议走 GeckoLib + bedrock）|
| 行为 | 4 颗、半径 1.4、高度 +1.0、80 tick 转一圈；每 10 tick 由**轮到的那颗球**电一次 3.5 格内的敌人（5 伤，各球错开闪烁）；电击时从球到目标画一条电弧 ✓ |
| 生命周期 | 主人没了 / 死了 / 换维度 / **buff 掉了** → 自己 `discard()` ✓；`TnSpellMechanics.maintainOrbs` 每 tick **按槽位**补齐＋去重（只看数量的话两颗球会重叠在同一角度 ✗），buff 消失时 `removeOrbs` 清场（双保险）|
| 客户端接线 | `client\TNSpellOrbClientEvents`（MOD 总线 ＋ Dist.CLIENT）：`RegisterLayerDefinitions` ＋ `RegisterRenderers` —— 少前者**一渲染就崩**、少后者**隐形** ✗ |
| 尺寸旋钮 | `TNThunderOrbRenderer.SCALE`（默认 1.5 → 约 0.56 格）|
| ★ 复用价值 | 这就是**暗系召唤线**的模板 ✓：纯服务端操控位置、客户端只画外观的召唤物已经跑通，将来只需加 AI / 目标选择 |

#### 3.2e 魔法阵 + 爆炸冲击波（2026-09-22 作者指定）

作者："可以绘制一个魔法阵吗，在释放雷球的**传说级和神级**魔法时地上留下魔法阵" +
"爆炸的效果很差，我没有感受到**爆炸波冲击出去**的那种感觉" + "球的粒子量还可以增加"。

| 项 | 内容 |
|---|---|
| 魔法阵贴图 | `assets/tnc/textures/entity/magic_circle.png`（128×128 透明背景，青环 ＋ 紫六芒 ＋ 金符点）—— 由 **`tools/gen_magic_circle.ps1`** 画（纯 ASCII 脚本 ✓ 无第三方素材）；预览 `docs\魔法阵_贴图预览.png` ✓ |
| 魔法阵实体 | `tnc:magic_circle`（`magic\TNMagicCircleEntity`）：**贴地的一张面片** ✓（不像粒子那样只是"一圈点"✗）；不可选中、无碰撞、不存档；半径/时长走 `SynchedEntityData` ✓，年龄用原版 `tickCount` ✓ |
| 何时出现 | `TnSpellMechanics.onSpellCast`：**tier 4 `explosive_thunder_orb`（传说级）半径 3.2 格 / 140 tick**、**tier 5 `cataclysm_thunder_orb`（神级）半径 5.0 格 / 220 tick** ✓（tier 名见 `Element.tierName`：4=传说级、5=神级 ✓）|
| 渲染 | `client\TNMagicCircleRenderer`：`entityTranslucentEmissive`（发光半透明 ✓）＋ 缓慢自转 ＋ 先亮后淡；**两个绕序各画一遍** ✗ —— 平面＋背面剔除，朝向判断反了就会整个看不见，而我没法在开发环境验证，所以两边都画保证可见 ✓ |
| **爆炸冲击波** | `tnc:shockwave`（`magic\TNShockwaveEntity`）由法术 JSON 的 **`SPAWN` 冲击动作**在爆炸点生成 ✓：前 10 tick 把 3→19 格内实体**向外推**（越近越强、略微上掀 ✓ `push` + `hurtMarked`），渲染成**向外扩张的光环**（同一张贴图，半径 30%→100% 快速淡出 ✓）|
| **震屏** | `client\TNSpellClientVisuals`：每客户端 tick 就近侦测冲击波实体（20 格内），越近越猛，加到 `ViewportEvent.ComputeCameraAngles` 的 yaw/pitch/roll 上并衰减 ✓ —— **不需要网络包**（实体本来就同步给客户端 ✓）|
| 爆炸粒子加强 | 全部**向外速度**大幅提高（命中 1.5~4.0、范围 2.0~5.0、CIRCLE 环 2.5~6.0）＋ 新增 `minecraft:large_smoke` 尘土环（SPHERE/CIRCLE/PILLAR）✓；击退 knockback t4 1.2→**2.0**、t5 1.8→**3.0**；范围半径 t4 12→**14**、t5 16→**20** ✓ |
| t5 模型/尺寸/速度 | `model_id` → **`tnc:projectile/lightingball`**（作者指定）＋ `scale 75`（0.375×75 ≈ **28 格直径** ✗）＋ `velocity 0.25→0.075`（**慢 70%** ✓）。⚠️ 28 格比一个区块还大 —— 按"体积 ×100"（∛100=4.64，以原来那颗 6.11 格的球为基准）算出来的；嫌过头就把 75 改成 20（7.5 格）/ 32（12 格）✓ |
| 球上粒子再加量 | t4 拖尾 110→**170**（紫 70/黄 70/弧 30）；t5 拖尾 136→**214**（紫 80/黄 80/黑 14/弧 40）—— 紫黄仍五五开 ✓ 黑仍≈9% ✓ |
| **第二轮（同一天，作者指定）** | ① **粒子改成"球的边缘一圈、量很大"** ✓：用 `shape: CIRCLE` ＋ **`extent`（环半径，单位格）** —— t4 球半径 1.6 格 → `extent 1.6`、t5 球半径 14 格 → `extent 14.0`；每档 3~4 个环批次（t4 黄150＋紫150＋弧40；t5 黄220＋紫220＋黑20＋弧60）✓<br>② **爆炸改成"从中心散开一大堆粒子"** ✗ 之前那套环/柱/尘土被作者否掉了：现在只有 `SPHERE`（t4 命中 700＋范围 1000；t5 命中 970＋范围 1520，速度 2~7）＋ `flash` ✓<br>③ **魔法阵改纯黄 ＋ 放大 4 倍**：贴图重画成纯黄（`tools/gen_magic_circle.ps1`，只用一个色相 ✓）；半径 t4 3.2→**12.8 格**、t5 5.0→**20 格**，时长 170/260 tick ✓<br>④ **t5 只留一颗球**：`extra_launch_count` 8→**0** ✓（"超级无敌大雷球"是一颗；顺带避免 9 颗球各喷 200+ 粒子的卡顿 ✗）<br>⑤ **触地即爆**：引擎没有 pierce/bounce ✓ 撞到地形就触发 impact/area_impact，本来就是"刚触地就炸" ✓ |
| ⚠️ `extent` 未实测 | 这是"环半径"字段（`ParticleBatch.extent` ＋ `EXTENT_TRESHOLD` ✓），但**我不知道它的单位是不是"格"** ✗（包里 0 个 CIRCLE 用例可参照）。若游戏里环明显偏小/偏大，把 `extent` ×16 或 ÷16 试试 ✓ |
| **第三轮（同一天，作者指定）** | ① **链序对调**：`explosive_thunder_orb` 4→**t3**、`orbiting_thunder_orb` 3→**t4** ✓（`SpellCatalog` 里两行 ＋ 两个 JSON 的 `learn.tier`；魔力/学习花费按 tier 自动跟着变 ✓）<br>② **环绕的球就是爆炸雷球**：环绕实体渲染尺寸 `SCALE` 1.5→**8.55**（= `explosive_thunder_orb` 的 `scale`，约 **3.21 格**）✓；**离人物更远**：环绕半径 1.4→**4.0 格**、高度 1.0→**1.6 格** ✓；电击判定改成**以球自己为中心**（球挂在 4 格外，再用玩家位置判定会打不到 ✗）；命中时在球心炸一圈小粒子 ✓<br>③ **爆炸改成"向外散射"**：粒子向外速度大幅提高（t3 **4~16**、t5 **5~18**）＋数量 1400+/2000+ ✓；范围半径 **30（t3）/ 40（t5）** ＋ **`distance_dropoff: NONE`**＝**同等伤害、不随距离衰减** ✓ —— 这就是作者要的"粒子触碰敌人造成同等爆炸伤害、半径非常大" ✓<br>④ ⚠️ 平衡：t3 现在是一发 **30 格无衰减 AoE**、t5 **40 格** ✗（一键清场）。按作者要求实现 ✓，想收敛只改 `radius` 与 `dropoff` ✓<br>⑤ 说明：**粒子本身不打伤害** —— 伤害来自引擎的 `area_impact`（不衰减 ✓），视觉上就是"粒子飞出去、范围内的敌人一起掉血" ✓；真要让单个粒子打伤害需要弹片实体（成本高，先不做 ✗）|
| **第四轮（同一天，作者指定）** | ① **环绕再远一点**：环绕半径 4.0→**6.0 格** ✓；**转速快一倍**：`ORBIT_PERIOD` 80→**40** ✓<br>② **环绕要有"同款粒子特效 ＋ 魔法阵"**：环绕实体每 tick 在**球边缘**（半径 1.6 格 ＝ 球半径）画环（`TnSpellMechanics.ring`：水平环 ＋ 慢慢转的竖直环，紫黄各半 ✓）；施法时也铺魔法阵（半径 **8.0 格** / 200 tick）✓<br>③ **t5 速度再快一倍**：`velocity` 0.075→**0.15** ✓<br>④ ★ **修"大雷球粒子不跟随"**：引擎的 `travel_particles` 在这颗球上不跟随 ✗ —— 改成 **Java 侧自己贴**：施法时开一个 20 秒窗口（`BIG_BALL_UNTIL`），每 tick 用 `SpellProjectile.getOwner()` 找到**自己那颗球**，在它的位置画半径 **14 格**（＝球半径）的环 ✓；同时把 JSON 里的 `travel_particles` 删掉 ✗（避免那份"留在原地"的粒子继续捣乱）|
| **第五轮 — 撤回 + 加速（同一天，作者："环绕很漂亮" ✓ ＋ "这个超级无敌大雷球还是不行啊，**还在抽**，粒子特效也一般，你要不**撤回再弄加速**？"）** | ① **速度撤回**：`velocity` 0.35→**0.45**（＝最早的原始值 ✓）。前四轮把它从 0.45 一路压到 0.075/0.15/0.35 想"慢 70%"，越慢"抽"越明显 ✗ —— 慢速时陨石在空中的每一帧都被重力/朝向修正放大，看起来就是抖 ✓<br>② **投放高度 14→24 格**：`scale 75` 的球直径 ≈28 格，从 14 格高投放等于**半个球埋在地里** ✗（这也是"抽"的观感来源之一 —— 与地形互相穿插）。抬到 24 格后球底离地 ≥10 格 ✓<br>③ **`travel_particles` 加回来 ✓**（第四轮为了修"不跟随"整段删掉了 ✗，作者的评价是"粒子特效也一般"）—— 引擎这份是贴在投射物上的，弧/黄/紫各 90（`extent 14` ＝ 球半径）＋ 内核 弧/黄 各 60 的 `SPHERE` ✓<br>④ ⚠️ **诚实的边界**：抽的根因我**没法在开发环境验证** ✗。若 0.45 ＋ 24 格后**还抽**，那就不是速度问题，而是引擎 `METEOR` 这条投放路径本身 —— 下一步换成**我们自己的下落实体**（照 `TNThunderOrbEntity` 的模板：服务端自己控位置，客户端只画外观 ✓，这样任何速度都稳 ✓）|
| **第五轮 — 大雷球粒子升级成球壳 `ballShell`** | 上一版只在赤道画**一圈**粒子 ✗（28 格的球配一条细线，不像电球）。新增 `TnSpellMechanics.ballShell(level, center, radius, time)`：按**纬度**叠 **5 层水平环**（半径 `radius·cos φ`，点数 `半径×4`、最少 12），每层复用 `ring` 自带的旋转经线 → 一整层**会转的球壳** ✓。半径 14 格时约 350 颗/tick（避开卡顿）✓；环绕雷球实体仍用单环 `ring` ✓（作者已确认好看，不动 ✗）|
| ★ 复用：`TnSpellMechanics.ring` | 一处实现，三处用：环绕雷球实体、大雷球跟随、以后任何"球边缘一圈"都可以直接调 ✓（参数：中心、半径、点数、时间）|
| ⚠️ 未验证的路径 | **引擎的 `SPAWN` 冲击动作在整合包里 0 个用例** ✗（我把 pack 的 resources 全搜过）—— 若它静默不生成冲击波实体，粒子层面的"向外爆开"仍然在 ✓；届时我改成 Java 侧直接生成（机制层已经知道"哪次施法是大招"✓，只差"何时命中"这个信号 ✗）|



#### 3.2c 雷球链 4 档的尺寸 / 粒子 / 爆炸（2026-09-22 作者指定）

> ⚠️ 本表是**第一次调优**的数字；**t5 后来又改过**（模型换成 `lightingball`、`scale 75`、速度 0.075），
> 以 **3.2e 节**为准 ✓。

| 档 | 法术 | 尺寸（`scale`）| 实际大小 | 粒子 |
|---|---|---|---|---|
| t1 | `thunder_orb` | 0.8 → **1.37** | 0.30 → **0.51 格** | 拖尾 18→65（两批弧）／命中 40→80 |
| t2 | `great_thunder_orb` | 2.8 → **4.79** | 1.05 → **1.80 格** | 拖尾 4→38（CIRCLE+SPHERE）／命中 40→100 |
| t4 | `explosive_thunder_orb` | 5.0 → **8.55** | 1.88 → **3.21 格** | 拖尾 **110**（紫 45 ＋ 黄 45 ＋ 弧 20）／命中 180／范围 440 |
| t5 | `cataclysm_thunder_orb` | 2.4 → **6.51** | 2.25 → **6.11 格** | 拖尾 **136**（紫 50 ＋ 黄 50 ＋ **黑 10** ＋ 弧 26）／命中 220／范围 825＋雷柱 |

- ★ **粒子一律加在"球"上，不加在"人"上** ✗（作者 2026-09-22："粒子量应该加在球上而不是人上，太挡视野了"）：
  彩粒子**只放 `client_data.travel_particles`**（跟着投射物走 ✓）；
  `cast.particles`（`origin: FEET`，也就是**施法者身上**）**只留那一道 `count: 1` 的电弧** ✗ —— 之前我把加量后的 `witch`/`squid_ink` 也放了一份在 cast 里，直接糊住第一人称视野。
- ★ **配色比例**（作者指定）：**紫 : 黄 ≈ 五五开** ✓；**黑只占一点点**（t5 拖尾 10/(50+50+10) ≈ **9%**，命中 20/220，范围 40/825）✓。粒子 id：紫 `minecraft:witch`、黄 `minecraft:electric_spark`、黑 `minecraft:squid_ink`（都是原版 ✓）。
- **爆炸/冲击波**：t4 范围半径 10→**12**、t5 12→**16**；范围粒子用 `SPHERE` 高速向外（0.5~1.8）＋ `CIRCLE` 环（1.2~2.4 / 1.5~3.0）＝向外扩散的冲击波观感 ✓；t5 再加一股 `PILLAR` 雷柱（触地那下）✓；击退 `knockback` t4 0.5→**1.2**、t5 1.0→**1.8**（玩法上的冲击波）✓
- **触地就爆炸**：引擎**没有**"撞地开关"（`ProjectileData` 只有 `divergence/homing_angle/perks`，`Perks` 只有 ricochet/bounce/pierce/chain_reaction ✗）—— 撞到地形触发 `impact`/`area_impact` **本来就是默认行为** ✓，所以这条无需改；t5 速度 0.45→**0.25** 让它慢慢砸下来更明显 ✓
- ⚠️ **"体积 ×5 / ×20" 的理解**：按**体积**算（边长 ×∛5=1.71、×∛20=2.71）。若作者本意是**边长**直接 ×5/×20，只需把 4 个 `scale` 改成 4.0/14.0/25.0/48.0（t5 ≈45 格，比一个区块还大 ✗ 我判断那不是本意）
- ⚠️ **粒子 id 拼错是静默的** ✗（引擎找不到就只是不生成）：改完务必核对拼写。当前用到的合法 id：`minecraft:witch` / `minecraft:electric_spark` / `minecraft:squid_ink` / `minecraft:flash` / `spell_engine:electric_arc_a` / `spell_engine:electric_arc_b` ✓
- ⚠️ `tools\verify_spells.ps1` 的 **D 段交付链检查已过期** ✗：它还在找"卷轴物品 / spellanvil tag / addSpell 绑定 / 卷轴 tooltip"——那是**已被魔法石取代**的旧路线，所以现在会对 74 个法术里的 66 个报假问题；A/B/C 三段（字段/枚举/必需字段）仍然有效 ✓，我这次的改动过 A/B/C ✓（下一轮修 D 段）

### 3.3 投射物模型（★ 本专题最有价值的成果）

**已验证的 6 步配方** → 详细步骤见 `docs/投射物模型_配方.md`（**必读** ✓）：
```
① 模型  assets/tnc/models/projectile/<名字>.json   贴图引用必须写 tnc:spell_projectile/<名字> ✓
② 贴图  assets/tnc/textures/spell_projectile/<名字>.png      ← 目录必须是 spell_projectile ✗ 不是 projectile
③ 登记  TNProjectileModels 里 CustomModels.registerModelIds(反射)
④ 烘焙  TNModelBaking 里 ModelEvent.RegisterAdditional      ← ★ 漏了这步永远紫黑
⑤ 法术  "model_id": "tnc:projectile/<名字>"（法术要在 jar 里）
⑥ 禁忌  kubejs 不能有同名法术副本
```
现有模型（**全部是用户自制** ✓）：`fireball` `waterball` `lightingball` `thunder_ball`。
> ⚠️ 注意拼写：雷系的号是 **`lightingball`**（**少一个 n，是历史笔误但已写进 19 个法术 JSON**）✗
> **别为了"修正"改这个 id** ✗ —— 改名等于让那 19 个法术的 `model_id` 全部失效 → 集体紫黑方块。

### 3.4 剧情道具「宝箱传说残卷」六卷（★ 不是投射物模型，但也是本专题做的）

| 项 | 说明 |
|---|---|
| 道具 | `tnc:canjuan_1a / 1b / 3 / 4 / 5 / 6`（卷一按作者要求拆上下卷），`stacksTo(1)`，已在 TN-C 创造页 ✓ |
| 外观 | 原版 `item/generated` 平面模型；六卷**统一一张"残页"贴图**（一部残卷散成碎片的观感）✓ |
| **右键阅读** | 复用**原版书本界面** ✓（`BookViewScreen`）：不画新 UI、不加贴图，翻页/排版/音效都是原版行为 ✓ |
| 正文来源 | `剧情\宝箱传说残卷.md` 的 **【卷文】段**（**文书专题**维护，是唯一事实来源）→ 由 `tools\gen_scroll_lang.ps1` 生成 lang 键 `scroll.tnc.<id>` ✓ |
| ⚠️ 绝不能进游戏 | **【暗扣】段**（对应角色、埋的钩子、真名）✗ —— 脚本**只取每个卷的第一段**；装机后核对过：`暗扣 / 吴归衡 / 公孙衍 / 熙永槐 / 庄鹊让 / 周坐望` 在 jar 内出现 **0 次** ✓（`鲁阳公` 是正文里作者故意留的唯一真名 ✓）|
| 排版硬约束 | 原版正文宽 **114px**、一页最多画 **14 行**，超出**静默裁掉** ✗；所以客户端自己折行后**每页只放 13 行** ✓ |
| 关键字样式 | 正文里的 **「」** 按编剧要求上**金色** ✓（"「」里的字可以用略不同的颜色"），不做解释 |
| 文案改字流程 | 改 md → 跑 `tools\gen_scroll_lang.ps1` → 重新编译装机（**不用碰 Java** ✓）|
| 注册时机的坑 | `DeferredRegister` 必须由 `TNMod` 构造函数**显式**挂总线 ✗ 不能靠静态块（类懒加载 → 静默不注册，jar 里有模型有贴图但游戏里没有 ✗）|

### 3.5 庄鹊让 · 剧情 NPC（2026-09-22，作者要求"先当 NPC 做"）

| 项 | 说明 |
|---|---|
| 实体 | `tnc:zhuangquerang`（`npc\ZhuangquerangNpcEntity`，继承 NPC 专题的 `TnDialogueNpc`）✓ |
| 模型 | **保持女仆模型** ✓：装了 GeckoLib 就用 `ZhuangquerangMaidNpcEntity` + `client\ZhuangquerangMaidGeoModel/Renderer` **直接画 TLM 的女仆模型**（`assets/tnc/geo/entity/zhuangquerang.geo.json` ＋ 128×128 女仆贴图 ＋ 我们自己写的轻微 idle）；没装才降级成原版人形 ＋ `zhuangquerang_humanoid.png`（`tools\gen_zhuangquerang_skin.ps1`）✓ |
| ⚠️ 构建依赖 | `libs\touhoulittlemaid-1.5.2.jar` `libs\geckolib-4.8.4.jar` ＋ `build.gradle` 的 `compileOnly fg.deobf(...)`；**`fg.deobf` 不认 `files(...)`** ✗，必须先加 `flatDir { dir 'libs' }`。两个 jar 只是编译用，不进我们的 jar ✓ |
| 剧本 | `zhuangquerang_first.txt` → `@next` → `zhuangquerang_second.txt`（**任务系统 BBB 代拟第二段 + 蓝调对话框主题色** ✓；台词归编剧系统 AAA ✗）|
| 位置 | `NpcPlacementDefaults` `CENTER (+15,0,-13)` **暂定** ✗；`VERSION` 已 +1 到 3（否则老存档不补位 ✗）|
| 加 NPC 的 6+2 处 | 实体注册 / 刷怪蛋 / **属性表**（漏了进游戏就炸）/ **渲染器**（漏了隐形）/ 创造页 / **`purge all` 硬编码名单** / 默认位置 / lang —— 详情见 `当前状态.md` 4.1 |
| 二周目 | 作者要她二周目变成"可选可玩角色"，没被选就仍是 NPC ✗ **未实装**，4 条待定项见 `当前状态.md` 4.2 |
| 与 NPC 专题的关系 | 代码在 `com.tnc.tnc.npc`（他们的目录）；我按他们的范式加**一个** NPC + 该动的接线，**没有重构他们的东西** ✓ |

### 3.6 一周目正史记录纸（五张：前情 ＋ 主线四段，2026-09-22）

作者要求："**类似残卷那样，记录主线四场的主要剧情和前情**"。

| 项 | 说明 |
|---|---|
| 道具 | `tnc:zhengshi_qianqing` / `zhengshi_1` / `zhengshi_2` / `zhengshi_3` / `zhengshi_4`（`magic\TNRecords`）✓ 已在 TN-C 创造页 |
| 机制 | **完全复用残卷那套** ✓：`TNScrollItem` 右键读 + `client\TNScrollScreen`（原版书本界面 + 「」金色）；代码没有第二份 ✗ |
| 正文来源 | `剧情\主线概要.md`（**编剧系统 AAA** 维护）→ `tools\gen_records_lang.ps1` 生成 `scroll.tnc.zhengshi_*` + 物品名 ✓（路径写在 `tools\records_lang_extra.json` 的 `doc` 里，脚本保持纯 ASCII ✓）|
| 外观 | 五张共用 `assets/tnc/textures/item/zhengshi.png`（MCA 的"文书"＝两张纸＋火漆印），**故意与残卷的单张撕页区分** ✓ |
| ⚠️ 元注释 | 概要是**交接文档**，有几句写给制作组看；脚本按 `strip` 名单删掉并在输出里列出（当前 2 处）✗ 不是静默改 |
| ⚠️ 守卫范围 | 残卷的"正文绝不点名"守卫现在**只扫 `canjuan_*`** ✓ —— 正史纸按设计就是全名叙述（已做反证测试 ✓）|
| ⚠️ 发放时机 | 段三/段四两张写的是结局（**归杀衍 / 归消失**）→ 早期掉落会剧透 ✗；**通完某段才给那张**，或改成"只写到当前进度"版，**等作者定** |

---

## 四、待办 / 已知问题

| 项 | 说明 |
|---|---|
| **火射线模型** | 5 个射线法术现在临时用 `fireball`（球 ✗）；等用户导出细长条模型（建议名 `fireray`）→ 按六步接上 |
| `waterball` 形状 | 现在是"火球形状 + 水贴图"✗；用户导出自己的模型即替换 |
| 土 / 暗 / 风系模型 | 现在都用 `lightingball`（外观相同 ✗）；按六步各配一个即可（`earthball` / `darkball` / `windblade`）|
| **70 张法术图标缺失** | 只有 35/105（`assets/tnc/textures/spell/<法术id>.png`）→ 缺的显示品红方块 ✗；`docs/美术资产清单.md` 列了缺哪些 |
| **雷/火/风各缺第 4 条链** | **等用户给规格** ✗（别自己编）|
| 风系链 2 第 5 级 | 占位名"风灵之主" |
| 暗系召唤线 | 现在是绕身球过渡版 ✗；要真召唤需自定义实体 + AI。**2026-09-22 已有模板** ✓：`TNThunderOrbEntity`（纯服务端控位置、客户端只画外观的召唤物，见 3.2d）——只差"加 AI / 选目标" |
| 特殊魔法 | 4.5/5；缺"每元素一个领域法术本体"（等用户定名字与效果）+ 亲和力加成数值可调（`TNSpecialMagic.BONUS_PER_POINT`）|
| 残卷掉落 / 阅读 | **道具本体 + 右键阅读已完成** ✓（`TNScrolls` / `TNScrollItem` / `client\TNScrollScreen`，正文由 `tools\gen_scroll_lang.ps1` 从剧情文档生成）；**箱子掉落**属于任务系统 / 文书专题 ✗ |

---

## 五、文档索引

| 文档 | 内容 |
|---|---|
| `docs/法术专题_交接.md` | **本文** |
| `docs/投射物模型_配方.md` | ★ 自制投射物模型的 6 步 + 紫黑排查顺序 |
| `docs/法术总表_按设计文档.md` | 六系全部法术名（对照设计总纲）|
| `docs/法术制作与测试.md` | 法术 JSON 的三种模板 + 测试循环 + 美术路径 + 6 个静默失效坑 |
| `docs/特殊魔法_设计.md` | 领域魔法方案与进度 |
| `docs/美术资产清单.md` | 逐法术列图标/模型/是否占位 |
| `docs/实施进度.md` | 全局进度与卡点 |
| `docs/项目交接.md` | 整个模组的交接（含目录导航、全部命令）|
| `PROJECT-STATE.md` §6/§7 | 引擎能力（模板/CLOUD/orientation/原版效果/学派名/音效）+ 方法论六条 |
| `D:\查看\TN-C_idea\Mc - Idea_TN-C.md` | 设计总纲原文 |
| 用户朋友的 `紫黑方块排查手册.md` | 另一份独立排查手册（坑 4 = 借用已注册路径，本项目最终用"注册自己的模型 + 强制烘焙"解决 ✓）|

---

## 六、环境坑（每一条都真踩过）

1. **`JAVA_HOME` 被 PCL 占用 → gradle 静默不编译** ✗，而 `identical: True` 只是两个旧文件互相比 ✓ → 已固定 `D:\ModTest\.jdk17` ✓，且**必须核对 jar 内容** ✓
2. **打包 zip 必须用正斜杠条目名** ✗（Windows 默认反斜杠 → MC 完全读不了 → 资源包"启用却无效"）
3. **kubejs 与 mod jar 同 id 时以 kubejs 为准** ✗（数据包后加载）→ 用了模型的法术只留在 jar ✓
4. **不要改 Fabric 模组的 jar** ✗（Sinytra Connector 重映射会失败 → 启动崩溃）
5. **`sync.cmd` 是双向的** ✗：删了 kubejs 里的文件后不要再跑 sync ✓，否则会从游戏侧"恢复"回来 → 再次覆盖 jar ✗
6. **`tools/*.ps1` 必须纯 ASCII** ✗（中文会被 GBK 读法吞换行导致解析失败）
7. **改模型贴图引用时注意目录**：引擎只认 `textures/spell_projectile/` ✓（写成 `textures/projectile/` 会"模型在、贴图找不到"→ 紫黑 ✗）
8. **深色 UI 里 `{}`/引号会破坏 PowerShell**：脚本里别在单引号字符串里混 `{` ✓，提交信息里别用 `"` ✓（用 `-F 文件`）
